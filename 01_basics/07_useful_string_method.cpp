#include <iostream>
using namespace std;
int main()
{
  string name;

  cout << "Enter your name:";
  getline(cin, name);

  //   cout << name.length();   //give length of character
  // cout << name.empty(); // return boolean value check string empty or not is empty 1 and not 0;
  // name.clear(); //clear or delete the name
  // cout << name.append("@gmail.com");  // appended the string or concartinate
  // cout << name.at(1);  //give then 1 index character ;
  // cout << name.insert(1, "$");  //insert characer with given index position ;
  // cout << name.find('u'); // find the index position of character

  //  cout << name.erase(0, 3); // erase characer give first number is the beginning index the second is ending index
}